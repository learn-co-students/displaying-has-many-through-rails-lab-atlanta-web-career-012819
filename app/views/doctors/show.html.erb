<h1><%= @doctor.name %></h1>
<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= link_to appt.patient_name, patient_path(appt.patient) %>
       - <%= appt.readable_datetime %></li>
  <% end %>
</ul>